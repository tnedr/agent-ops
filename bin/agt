#!/usr/bin/env python3
"""Entrypoint script for agt command (fallback if not installed via pip)."""

import sys
from pathlib import Path

# Add parent directory to path to allow importing agt
sys.path.insert(0, str(Path(__file__).parent.parent))

from agt.cli import main

if __name__ == "__main__":
    main()

